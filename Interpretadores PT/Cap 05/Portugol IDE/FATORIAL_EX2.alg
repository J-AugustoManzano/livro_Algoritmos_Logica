// programa FATORIAL_EX2
inicio
  variavel inteiro CONT, FAT 
  FAT <- 1
  CONT <- 1
  enquanto nao (CONT > 5) faz 
    FAT <- FAT * CONT
    CONT <- CONT + 1
  fimenquanto 
  escrever "5! = ", FAT
fim
