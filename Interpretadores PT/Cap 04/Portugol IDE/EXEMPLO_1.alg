// programa EXEMPLO_1
inicio
  variavel real N, R  
  ler N
  se nao (N < 0) entao
    se (N =/= 5) entao
      R <- N ^ (1.0 / 2.0)
    senao
      R <- N ^ (1.0 / 3.0)
    fimse
    escrever R 
  fimse
fim

